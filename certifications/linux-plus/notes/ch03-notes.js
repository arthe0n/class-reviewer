window.ReviewApp.content.register({
type: "notes",
cert: "linux-plus",
chapter: "Ch 03 · Files, Directories & Search",
items: [
{
title: "Linux Files, Paths, Commands & Search — Exam Reference",
body: "# Linux Files, Paths, Commands & Search\n\n## 1. Paths and Directory Structure\n\nLinux presents storage as **one virtual directory tree** rooted at `/`. Other filesystems are mounted into this tree.\n\n| Concept | Key point |\n|---|---|\n| **Absolute pathname** | Full path starting at `/`, e.g. `/etc/passwd` |\n| **Relative pathname** | Path interpreted from the current working directory |\n| **Home directory** | `~` for the current user; `~mary` for Mary's home directory |\n| **Current directory** | `pwd` displays it |\n| **Change directory** | `cd <path>`; plain `cd` returns home; `cd -` returns to the previous directory |\n| **Parent directory** | Directory one level closer to `/` |\n| **Tab completion** | Bash completes a unique pathname with `TAB` |\n\n### Important directories\n\n| Path | Purpose |\n|---|---|\n| `/` | Root filesystem |\n| `/boot` | Kernel, initramfs, boot files |\n| `/boot/efi` | EFI System Partition / UEFI bootloaders |\n| `/etc` | System-wide configuration |\n| `/home` | User home directories |\n| `/root` | Root user's home directory |\n| `/usr` | Most system commands and utilities |\n| `/usr/bin` | User binary commands |\n| `/usr/sbin` | System binary commands |\n| `/usr/lib`, `/usr/lib64` | Libraries |\n| `/usr/local` | Local programs |\n| `/usr/share` | Architecture-independent files |\n| `/opt` | Optional / third-party software |\n| `/var` | Variable data |\n| `/var/log` | System/application logs |\n| `/var/lib` | Application state, databases, package information |\n| `/var/cache` | Cached application/package data |\n| `/tmp` | Temporary files |\n| `/srv` | Data served by system services |\n| `/mnt` | Temporary manual mount point |\n| `/media` | Auto-mounted removable media |\n| `/proc` | Virtual process/kernel information filesystem |\n| `/sys` | Virtual devices/kernel interfaces filesystem |\n| `/dev` | Device files |\n| `/run` | Runtime process state |\n\n## 2. Shell Metacharacters & Wildcards\n\n| Syntax | Meaning |\n|---|---|\n| `$` | Shell variable |\n| `~` | Home-directory variable |\n| `#` | Shell-script comment |\n| `&` | Background execution |\n| `;` | Command termination/separation |\n| `<`, `<<`, `>`, `>>` | Input/output redirection |\n| `\\|` | Pipeline |\n| `*` | Wildcard: anything / any number of characters |\n| `?` | Wildcard: one character |\n| `[ ]` | Wildcard range |\n| `'...'` | Literal text |\n| `\"...\"` | Allows variables |\n| `\\` | Escape character |\n| `( )`, `{ }` | Command grouping |\n\n**Wildcard vs. regex:** shell wildcards are filename-expansion patterns such as `*.txt`; `grep -E` uses **extended regular expressions**, while `grep -F` treats the pattern as literal text rather than a regular expression.\n\n## 3. Files and Filenames\n\nLinux files may be **text, binary, executable programs, directories, linked files, device files, named pipes, or sockets**. Directories are special files used to organize and locate other files.\n\n- Filenames can contain up to **255 characters**.\n- Filenames beginning with `.` are **hidden**.\n- Linux **does not require filename extensions**.\n- Avoid shell metacharacters in filenames.\n\n### `ls -F` file-type indicators\n\n| Indicator | Meaning |\n|---|---|\n| `/` | Directory |\n| `*` | Executable |\n| `@` | Symbolic link |\n| `=` | Socket |\n| `` ` `` | Named pipe (FIFO) |\n| Nothing | Regular file |\n\n## 4. `ls` — View Files and Metadata\n\n`bash\nls [OPTION]... [FILE]...\n`\n\n| Option | Purpose |\n|---|---|\n| `-a`, `--all` | Include hidden files |\n| `-d`, `--directory` | Show directory's own metadata instead of contents |\n| `-F`, `--classify` | Append file-type indicators |\n| `-i`, `--inode` | Show inode numbers |\n| `-l` | Long format: type, permissions, links, owner, group, size, time, name |\n| `-R` | Recursive directory listing |\n| `-h`, `--human-readable` | Human-readable sizes; commonly `ls -lh` |\n\n### Reading `ls -l`\n\nExample:\n\n`text\ndrwxr-xr-- 1 user staff 1200 May 13 script.sh\n`\n\n- First character = **file type**\n- Next 9 characters = permissions: **owner / group / others**\n- Then: **hard-link count → owner → group → size → modification date → filename**\n\nFile-type characters:\n\n| Character | Type |\n|---|---|\n| `-` | Regular file |\n| `d` | Directory |\n| `l` | Symbolic link |\n| `c` | Character device |\n| `b` | Block device |\n| `p` | Named pipe |\n| `s` | Socket |\n\n## 5. Create, Copy, Move & Delete\n\n### Create\n\n`bash\ntouch file\nmkdir directory\nmkdir -p parent/child/grandchild\n`\n\n- `touch` creates empty files or updates access/modification timestamps.\n- `mkdir -p` creates missing parent directories.\n- `mkdir -v` reports each directory created.\n\n### Copy\n\n`bash\ncp [OPTION]... <source> <dest>\n`\n\n| Option | Purpose |\n|---|---|\n| `-a` | Archive: recursive + preserve permissions, ownership, timestamps |\n| `-f` | Force overwrite |\n| `-i` | Prompt before overwrite |\n| `-n` | Never overwrite |\n| `-R`, `-r` | Recursive directory copy |\n| `-u` | Overwrite only if source is newer |\n| `-v` | Verbose output |\n\nA **source and destination are both required**. Copying directories requires `-R`/`-r`.\n\n### Move / rename\n\n`bash\nmv [OPTION]... <source> <dest>\n`\n\n`mv` can **move or rename** files/directories and can move + rename in one command.\n\n| Option | Purpose |\n|---|---|\n| `-f` | Force overwrite |\n| `-i` | Prompt before overwrite |\n| `-n` | Never overwrite |\n| `-u` | Overwrite only if source is newer |\n| `-v` | Verbose output |\n\n### `rsync`\n\nUsed for fast copies, many-file transfers, and backups; can copy over a network through OpenSSH.\n\n| Option | Purpose |\n|---|---|\n| `-a` | Archive mode (`-rlptgoD`) |\n| `-D` | Preserve device/special files |\n| `-g` | Preserve group |\n| `-h` | Human-readable output |\n| `-l` | Preserve symbolic links |\n| `-o` | Preserve owner |\n| `-p` | Preserve permissions |\n| `--progress` | Show transfer progress |\n| `-r` | Recursive copy |\n| `--stats` | Transfer statistics |\n| `-t` | Preserve modification times |\n| `-v` | Verbose output |\n\n### Delete\n\n`bash\nrm [OPTION]... <file>\nrmdir [OPTION]... <directory>\n`\n\n`rm`:\n\n| Option | Purpose |\n|---|---|\n| `-d` | Delete empty directories |\n| `-f` | Suppress prompts; ignore nonexistent targets |\n| `-i` | Prompt before each deletion |\n| `-I` | Ask once before large/recursive deletions |\n| `-R`, `-r` | Recursively delete directory trees |\n| `-v` | Verbose output |\n\n**Safety rule:** prefer `rm -i` when deleting files interactively.\n\n`rmdir` removes **empty directories only**; `rmdir -p` can remove a chain of empty directories and `-v` reports removals.\n\n## 6. Hard vs. Symbolic Links\n\n### Hard link\n\n`bash\nln <original-file> <linked-file>\n`\n\n- Multiple filenames share the **same inode and underlying data**.\n- Original must already exist.\n- New filename must not already exist.\n- Must be on the **same filesystem**.\n- Deleting one filename does not remove the data while another hard-link name exists.\n- `ls -i` can confirm matching inode numbers.\n\n### Symbolic (soft) link\n\n`bash\nln -s <original-file> <linked-file>\n`\n\n- Has a **different inode** from the target.\n- Points to the target's name/location rather than sharing its data.\n- Can cross filesystem boundaries.\n- `ls -l` displays the pointer with `->`.\n- `readlink -f <file>` resolves a chain to its final target.\n- A deleted/moved target creates a **stale/dead link**.\n\n## 7. Reading Text Files\n\n### Whole files\n\n`bash\ncat file\ncat -n file\n`\n\n`cat -n` adds line numbers.\n\n`pr` can format text, including side-by-side output with `-m`.\n\n### First / last lines\n\n`bash\nhead file\nhead -n 5 file\ntail file\ntail -n 5 file\ntail -f /var/log/example.log\n`\n\n- `head` defaults to the first **10** lines.\n- `tail` defaults to the last **10** lines.\n- `tail -f` follows a file as new lines are appended; stop with `Ctrl+C`.\n- On journald systems, use `journalctl --follow` to follow journal messages.\n\n### Pagers\n\n- `more`: forward-only pager; Space = next page, Enter = next line, `q` = quit.\n- `less`: more capable pager; supports backward movement and searching.\n- `less` is the default `man` pager in the notes.\n\nUseful `less` keys:\n\n| Key | Action |\n|---|---|\n| Spacebar | Forward one page |\n| `Esc` + `V` | Backward one page |\n| Arrow keys | Move up/down |\n| `/pattern` | Search forward |\n| `?pattern` | Search backward |\n| `q` | Exit |\n\n## 8. `grep` — Search Text\n\n`bash\ngrep [OPTIONS] <pattern> [FILE]...\n`\n\n| Option | Purpose |\n|---|---|\n| `-i` | Ignore case |\n| `-v` | Show nonmatching lines |\n| `-n` | Show line numbers |\n| `-c` | Count matching lines |\n| `-E` | Extended regular expressions (`egrep`) |\n| `-F` | Fixed/literal matching; no regex interpretation (`fgrep`) |\n| `-R`, `-r` | Recursive directory-tree search |\n| `-d skip` | Skip directories when encountered |\n\n**Default:** `grep` is **case-sensitive**.\n\nExample:\n\n`bash\ngrep -i christine /etc/passwd\ngrep -n error app.log\ngrep -R 'hosts:' /etc\n`\n\n## 9. Find Information on the System\n\n### File information\n\n`bash\nfile mytest\nstat mytest\n`\n\n- `file` = basic file type.\n- `stat` = detailed metadata: size, inode, device, access/modify/change timestamps, ownership, permissions, links.\n\n### Compare files\n\n`bash\ndiff file1 file2\ndiff -q file1 file2\ndiff -y file1 file2\n`\n\n- `diff` = line-by-line comparison.\n- `-q` = brief difference report.\n- `-y` = side-by-side output.\n- `-e` = generate an `ed` script to transform the first file into the second.\n- `-s` = report identical files.\n\n`sdiff` is another side-by-side comparison utility.\n\n### Locate commands/files\n\n| Command | Best use |\n|---|---|\n| `which command` | Full command path from `$PATH`; can also reveal aliases |\n| `whereis command` | Binary, source files, and man pages |\n| `locate pattern` | Fast search of a prebuilt database |\n| `find path ...` | Live recursive metadata-based search |\n\n`PATH` contains directories searched for command binaries; entries are separated by `:`.\n\n### `locate`\n\n`bash\nlocate pattern\nlocate -b passwd\n`\n\n- Searches a prebuilt database, typically `/var/lib/mlocate/mlocate.db` in the notes.\n- Database is typically updated **once per day**.\n- `updatedb` refreshes it manually and requires superuser privileges.\n- `-b` = basename only.\n- `-i` = ignore case.\n- `-c` = count matches.\n- `-A` = match against all supplied patterns.\n- `-q` = suppress error messages.\n- `-r` = regular expression.\n- `-w` = match the full pathname (default).\n\n### `find`\n\n`bash\nfind [PATH...] [OPTION] [EXPRESSION]\nfind . -name \"*.txt\"\nfind . -maxdepth 2 -name \"*.txt\"\n`\n\n`find` starts at the specified path and searches recursively.\n\n| Criterion | Purpose |\n|---|---|\n| `-amin` | Accessed less/more than specified minutes ago |\n| `-atime` | Accessed less/more than specified days ago |\n| `-empty` | Empty files/directories |\n| `-fstype` | Specific filesystem type |\n| `-group` | Group/GID ownership |\n| `-inum` | Specific inode |\n| `-mmin` | Modified less/more than specified minutes ago |\n| `-mtime` | Modified less/more than specified days ago |\n| `-name` | Filename |\n| `-regex` | Filename using regex instead of shell wildcards |\n| `-size` | Size less/more/equal |\n| `-type` | File type (`b c d p f l s`) |\n| `-user` | User/UID ownership |\n| `-maxdepth` | Limit search depth |\n\n### SUID audit example\n\n`bash\nfind /usr/bin -perm /4000\n`\n\n`4000` is the SUID bit. The leading `/` makes the permission search match that bit while ignoring the other permission bits.\n\n---\n\n## Exam Traps to Remember\n\n- **`~` ≠ `/`**: `~` is home; `/` is the root of the entire directory tree.\n- **`cd` vs. `cd -`**: home vs. previous directory.\n- **`ls -a`** reveals hidden files; **`ls -h`** makes sizes readable.\n- **`cp` needs a destination** and needs `-R`/`-r` for directories.\n- **`rm -r`** removes non-empty directory trees; **`rmdir`** does not.\n- **Hard link = same inode**; **symbolic link = different inode + pointer**.\n- **`grep` searches text**; **`find` searches filesystem metadata**.\n- **`locate` uses a database** and may be stale; **`find` searches the filesystem directly**.\n- **Shell wildcards** (`*`, `?`, `[ ]`) are not the same as **regular expressions** used by `grep -E` and `find -regex`.",
tags: ["paths", "filesystem", "ls", "wildcards", "grep", "cat", "head", "tail", "less", "find", "locate", "links"]
}
]
});
